name: docuscan_mobile
description: Native Flutter companion for the DocuScan Laravel API.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  flutter_secure_storage: ^10.0.0
  image_picker: ^1.2.1
  image_cropper: ^12.2.1
  pdf: ^3.11.3
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
